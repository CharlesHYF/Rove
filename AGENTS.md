# AGENTS.md
> 项目级 AI 指令文件。各主流 AI 编码工具进入项目时优先读取本文件。
>
> **本文件内联了硬约束原文，不是指针。** 动手写代码前必须读完本文件。

## 0. 交付红线（必须满足，否则不算完成）
- 交付前必须跑 `make verify`（先规范校验 `scripts/check.sh`，再跑测试 `scripts/test.sh`），**全绿才算完成**。
- 测试必须实跑并全部通过，禁止"看起来对"就交付。
- 代码提交到 `agents/feature/<描述>` 分支，禁止直接推 `main` 或发起 PR，禁止任何 AI 联合署名；author/committer 必须为 `Charles <w1400214654@outlook.com>`。
- **证据驱动**：凡事实或技术判断，先查实相关源码/文档/命令输出再作答，结论必须附依据（文件:行号、实跑输出）；不确定就明说、点出缺口，绝不编造。

## 1. 命名硬约束
- **Go**：请求 `XxxRequest`、响应 `XxxResponse`，内部 `XxxDTO`；模块名 `rove`，包路径按规格书 §4。
- **变量/函数见名知义**：禁止单字母（`i`/`j` 仅限循环索引）、`tmp`/`data`/`obj`/`item1` 等无意义名。
- **禁止魔法数字**：有语义的数字必须提为文件/包顶部具名常量（`MAX_REDIRECTS`、`MIN_BODY_BYTES` 等）；测试表数据字面量除外。

## 2. 文件头注释块（每个源码文件必须有，`scripts/check.sh` 校验"作用"与"创建日期"）
```go
/*
 * 文件作用：<一到两句话说明本文件职责，句号结尾>
 * 创建日期：YYYY-MM-DD
 * 修改日期：YYYY-MM-DD
 */
package xxx
```
- 文件头**只写作用，不写实现**。
- 包文档注释（`// Package xxx 定义...`）保留在 package 行下方（Go 文档注释标准）。

## 3. 禁用字符（`scripts/check.sh` 校验）
- **禁止**弯/角/全角引号，统一半角 `"` / `'`。
- **禁止** Unicode 破折号（EM DASH 等），统一半角双连字符 `--`。
- **禁止** Emoji 出现在代码、注释、文档、提交信息中。

## 4. 结构与格式硬约束
- **分层**：Rove 是 CLI/库项目（非 Web 服务），按实现规格书 §4 的模块布局分层：
  - `cmd/rove` 只做装配与参数校验，不写业务逻辑；
  - `internal/` 为项目私有（config/runtime/app/scheduler）；
  - `pkg/` 按域分层（document/fetch/content/...），接口与实现分离；
  - 禁止跨层/循环依赖：上层可依赖下层，下层不得反向依赖上层；平级模块只通过接口调用。
- **强制大括号**：`if`/`else`/`for`/函数体即使一行也要 `{}`；代码块前后留空行。
- **键值/元素独占一行**：结构体/映射字面量每个字段一行；禁止并排。
- **缩进**：Go 用 gofmt（Tab）；YAML/JSON 2 空格（见 `.editorconfig`）。行尾 LF。
- **注释语言**：一律中文。
- **错误处理**：一律用 `fmt.Errorf("...: %w", err)` 保留调用链，禁止 `%v` 丢失类型；上层用 `errors.Is`/`errors.As` 判断。

## 5. 编码前置流程
- **模块文档闸门**：新模块/新功能**必写** `docs/modules/<系统模块>/<模块>.md`（含功能描述、接口、入参、返回），评审通过后再编码；小改/bugfix 豁免但需在 commit 说明。
- 需求边界、入参出参、异常场景没问清，不许开写。

## 6. 常用命令
```bash
make verify   # 交付前总闸门：规范校验 + 测试（全绿才算完成）
make lint     # 仅规范校验（禁用字符/文件头/必需文件/命名）
make check    # 仅跑测试
go test ./... # 单测
```
